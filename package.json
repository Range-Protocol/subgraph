{
  "name": "mantle-agni",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create": "graph create --node https://deploy.graph.fusionx.finance mantle-agni",
    "deploy": "graph deploy --node https://deploy.graph.fusionx.finance --ipfs https://api.thegraph.com/ipfs/ mantle-agni",
    "create-local": "graph create --node http://localhost:8020/ mantle-agni",
    "remove-local": "graph remove --node http://localhost:8020/ mantle-agni",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mantle-agni",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.42.3",
    "@graphprotocol/graph-ts": "^0.29.3"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
