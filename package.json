{
  "name": "range-protocol-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ polygon-quickswap",
    "create-local": "graph create --node http://localhost:8020/ polygon-quickswap",
    "remove-local": "graph remove --node http://localhost:8020/ polygon-quickswap",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 polygon-quickswap",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.42.3",
    "@graphprotocol/graph-ts": "^0.29.3"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
