{
  "name": "range-protocol-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mainnet-pancakeswap",
    "create-local": "graph create --node http://localhost:8020/ mainnet-pancakeswap",
    "remove-local": "graph remove --node http://localhost:8020/ mainnet-pancakeswap",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mainnet-pancakeswap",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.67.4",
    "@graphprotocol/graph-ts": "^0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
